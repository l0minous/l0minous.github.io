
  # Recreate with Black Background

  This is a code bundle for Recreate with Black Background. The original project is available at https://www.figma.com/design/YYCiUKnmOYfGEEYsIkRQLt/Recreate-with-Black-Background.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  